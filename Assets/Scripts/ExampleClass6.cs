using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExampleClass6 : MonoBehaviour
{
    
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("c");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
